# Case: Schema migration validation fails

## Input

The user asks the agent to upgrade an unversioned project.

## Repository context

The managed router block was added, but validation finds an unbalanced end marker before the version state has been committed.
