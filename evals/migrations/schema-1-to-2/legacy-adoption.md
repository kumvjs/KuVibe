# Case: Adopt an unversioned KuVibe project

## Input

The user replaces the old `kuVibe.md` with release 0.2.0 and asks the agent to continue normal work.

## Repository context

- `.agents/kuvibe.yaml` is absent.
- `AGENTS.md`, `.agents/project.md`, workflows, project docs, and historical notes exist.
- The legacy files contain project-specific knowledge.
