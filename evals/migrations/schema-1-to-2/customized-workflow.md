# Case: Adopt a customized legacy workflow

## Input

The user replaces `kuVibe.md` and asks the agent to upgrade the project.

## Repository context

- The harness has no version state.
- `.agents/workflow/development.md` differs from the old KuVibe template and contains team-specific deployment checks.
- Git history does not prove which lines are generated.
