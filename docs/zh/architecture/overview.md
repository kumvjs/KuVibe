# 架构概览

KuVibe 有意划分为可移植的 Markdown 协议和可选的确定性工具。智能体判断负责解释需求和作出工程选择；工具负责清单检测、项目框架验证和文档索引等可重复的事实检查。

这一边界既防止工具不可用时阻塞工程工作，也避免机械式检测器把建议伪装成事实。

项目中的 `.agents/kuvibe.yaml` 记录已安装的协议和 Schema，`kuVibe.md` 声明当前版本并提供受支持迁移。迁移逻辑属于文件协议；可选工具只读取和验证状态，不负责改写用户项目。
