# 用户体验

对于新项目，将 `kuVibe.md` 复制到仓库中，并让智能体初始化项目。智能体会建立上下文、约定、工作流、文档和相关适配器。如果技术方案尚未确定，智能体会调研三个理由充分的选项，并请用户做出一次选择。

对于现有项目，智能体会识别已经成立的事实，并保留安全的既有约定。它绝不会借初始化之名擅自迁移项目。

初始化后，用户只需直接描述变更。`AGENTS.md` 会自动引导智能体遵循既定流程。用户不需要使用强制 CLI、安装技能，也不必接触任务分级。

