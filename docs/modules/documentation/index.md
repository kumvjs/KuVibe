# Documentation

Markdown under `docs/` is the source of truth for how the product currently works. VitePress renders that source but does not own it.

Every meaningful change passes a documentation impact gate covering rules, workflows, data, interfaces, UI, operations, and architecture. Only pages with real content are created.
