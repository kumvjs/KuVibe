# Testing

Run:

```bash
pnpm check
pnpm test
pnpm docs:build
```

Type checking covers public contracts. Node's test runner verifies commands against isolated temporary repositories. The VitePress build catches broken configuration and Markdown rendering issues. Behavior evals provide declarative protocol coverage.
